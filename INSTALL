build instructions:
  - be sure to have OpenGL and Qt installed on your system
  - cd ~KubixRoot
  - cmake .
  - make

run kubix via:
  - cd ~KubixRoot
  - ./Kubix

clean directory:
  - cd ~KubixRoot
  - make clean
  - make distclean (to delete the temporary cmake files)

